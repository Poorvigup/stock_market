import 'package:flutter/material.dart';

// Defines the custom color palette for the application, inspired by the reference design.
class AppColors {
  static const Color scaffoldBackground = Color(0xFFEFF3F3); // Light minty off-white
  static const Color primaryDark = Color(0xFF3A4F51);      // Deep pine green/teal
  static const Color accentYellow = Color(0xFFD4A93F);     // Mustard yellow/gold
  static const Color cardBackground = Colors.white;         // Pure white for cards
  static const Color chartLine = Colors.cyan;               // The existing chart color
}